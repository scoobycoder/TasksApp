<h1>Listing Tasks</h1>

<table>
  <tr>
    <th>Task</th>
    <th>Complete</th>
  </tr>

  <% @task.each do |task| %>
      <tr>
        <td><%= task.title %></td>
        <td><%= task.text %></td>
        <td><%= link_to 'Show', tasks_path(task) %></td>
      </tr>
  <% end %>
</table>